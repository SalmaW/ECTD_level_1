/*void main() async{
  var x = 1;

  print(1);

  Future<int> => var y = await Future.delayed(Duration(seconds: 5), () {
    return 2;
  });
  var z  = x + y;
  // error - the data types are not the same, as x is int and y is Future<int>
  // to solve it use -await keyword- after the assignment operator and add -async keyword- after the parentheses of main method

  print(z);
}*/
