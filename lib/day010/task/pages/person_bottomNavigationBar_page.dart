import 'package:flutter/material.dart';

class PersonBottomNavigationBarPage extends StatelessWidget {
  const PersonBottomNavigationBarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:
          SafeArea(child: Center(child: Text("Person Page is Empty for now"))),
    );
  }
}
