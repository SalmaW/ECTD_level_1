import 'package:flutter/material.dart';

class PhoneBottomNavigationBarPage extends StatelessWidget {
  const PhoneBottomNavigationBarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Center(child: Text("Phone Page is Empty for now"))),
    );
  }
}
